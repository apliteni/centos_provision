- name: Add MariaDB repos
  template:
    src: "mariadb.repo.j2"
    dest: "/etc/yum.repos.d/mariadb.repo"
    force: yes

- name: Install supporting libraries
  package:
    name:
      - perl-DBI
      - libaio
      - boost-program-options
    state: installed

- name: Install MariaDB packages
  package:
    name:
      - MariaDB-server-10.3.32
      - MariaDB-client-10.3.32
      - MariaDB-tokudb-engine-10.3.32
    state: installed
    enablerepo: "mariadb"

- name: Install PyMySQL
  pip:
    name: pymysql
    state: present
    executable: pip3

- name: Ensure mariadb service is started
  service:
    name: mariadb
    state: started
    enabled: yes
