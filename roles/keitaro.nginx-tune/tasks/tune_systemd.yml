- name: Make systemd nginx extra config dir
  file:
    path: /etc/systemd/system/nginx.service.d
    state: directory

- name: Generate nginx systemd config
  template:
    src: set_limits.conf
    dest: /etc/systemd/system/nginx.service.d/set_limits.conf
  notify:
    - reconfigure nginx

