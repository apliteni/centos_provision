- include_role:
    name: _init/create-user-and-dirs

- file:
    path: "{{ nginx_log_dir }}"
    state: directory
    owner: root

- file:
    src: /bin/true
    dest: /bin/kctl
    owner: root
    state: link
