- name: Prune abandoned, broken & irrelevant certificates
  command: "{{ kctl_bin_dir }}/kctl certificates prune safe"
  changed_when: false

- name: Start renewing certificates job in background
  shell: "nohup {{ kctl_bin_dir }}/kctl certificates renew &>/dev/null &"
  changed_when: false
