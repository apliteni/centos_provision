#!/usr/bin/env bash
set -e -x -o pipefail

source ci/bin/lib/ensure_variables_are_set.sh

BUILDS_PATH="${1}"
TTL="${2}"

ensure_variables_are_set TTL BUILDS_PATH

mc rm -r --force --older-than ${TTL} default/${BUILDS_PATH}
