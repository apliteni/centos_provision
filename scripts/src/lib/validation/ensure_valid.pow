#!/usr/bin/env powscript


ensure_valid(option, var_name, validation_methods)
  error="$(get_error "${var_name}" "${validation_methods}")"
  if isset $error
    print_err "-${option}: $(translate "prompt_errors.${error}" "value=${VARS[$var_name]}")"
    exit ${FAILURE_RESULT}
