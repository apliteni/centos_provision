- name: Set default tracker language
  command: "{{ php_path }} bin/cli.php system:set_setting default_language {{ tracker_language | default('en') }}"
  args:
    chdir: "{{ keitaro_app_dir }}"
  become_user: "{{ keitaro_user }}"
  changed_when: false
  when: kctl_running_mode == "install"
