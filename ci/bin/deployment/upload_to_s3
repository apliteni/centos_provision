#!/usr/bin/env bash
set -e -x -o pipefail

source ci/bin/lib/ensure_variables_are_set.sh

ensure_variables_are_set LOCAL_BUILD_PATH S3_BUCKET DEPLOY_TO

mc cp --recursive "${LOCAL_BUILD_PATH}" "default/${S3_BUCKET}/${DEPLOY_TO}/"
