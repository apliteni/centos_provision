- name: Prepare keitaro home dir
  file:
    path: "{{ keitaro_root }}"
    state: directory
    owner: "{{ keitaro_user }}"
    group: "{{ keitaro_group }}"
    mode: u=rwX,g=rX,o=rX
    recurse: yes
