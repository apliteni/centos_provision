#!/usr/bin/env powscript


stage4()
  debug "Starting stage 4: generate inventory file"
  if isset $AUTO_INSTALL
    debug "Skip reading vars from stdin"
  else
    get_user_vars
  write_inventory_file
