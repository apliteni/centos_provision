- name: Obtain Let's Encrypt cert
  command: "{{ letsencrypt_command }} --domain={{ domain }}"

- name: Add managed https vhost config file
  template:
    src: vhost.j2
    dest: "/etc/nginx/conf.d/https_{{ domain }}.conf"
    mode: 0644
  notify:
    - reload nginx
