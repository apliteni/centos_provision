- include: upgrade_php.yml
  notify:
    - reload nginx

- include: upgrade_nginx.yml
  notify:
    - reload nginx

- include: upgrade_tracker.yml

- include: cleanup.yml

- name: Set installer_version
  command: sed -i 's/installer_version=.*/installer_version=1.1/g' /root/.keitaro/installer_config

