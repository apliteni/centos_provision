http:
  address: {{ php_rr_bind_address }}
  workers:
    command: "php {{ keitaro_root }}/server.php"
    pool:
      numWorkers: {{ php_tracker_rr_workers_count }}
      maxJobs: {{ php_tracker_rr_worker_max_requests }}
      allocateTimeout: 10
      destroyTimeout:  10

rpc:
  enabled: true

