---
- name: Remove current configs
  file:
    path: "{{ clickhouse_config_path }}"
    state: absent

- name: Create symlink to configs which generated by Keitaro
  file:
    src: "{{ keitaro_root }}/application/config/clickhouse"
    dest: "{{ clickhouse_config_path }}"
    state: link
  notify: restart-ch