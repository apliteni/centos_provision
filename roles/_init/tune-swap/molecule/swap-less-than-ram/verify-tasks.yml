- name: Get swap file stats
  stat:
    path: "/keitaro.1.swap"
  register: swap_path_info

- name: Test swap file is present
  assert:
    that: swap_path_info.stat.exists

- name: Test swap file size is 1g
  assert:
    that: (swap_path_info.stat.size / 1024 / 1024) | int == 1000
