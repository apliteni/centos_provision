#!/usr/bin/env bash
set -e -x -o pipefail

source ci/bin/lib/ensure_variables_are_set.sh

ensure_variables_are_set S3_HOST S3_KEY S3_SECRET

mc config host add default https://${S3_HOST} ${S3_KEY} ${S3_SECRET}
