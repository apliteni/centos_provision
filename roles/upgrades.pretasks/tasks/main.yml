- shell: 'true'
  register: no_tags_specified

- include: upgrade_php.yml
  when: no_tags_specified is not defined
  tags: [upgrade, upgrade_php]

- include: upgrade_server.yml
  when: no_tags_specified is not defined
  tags: [upgrade, upgrade_server]