- name: Migrate CH database
  command: "{{ kctl_path }} run cli-php ch_db:migrate"
  changed_when: false
  notify:
    - restart php-fpm
    - restart roadrunner

- name: Recreate CH mysql proxy tables
  command: "{{ kctl_path }} run cli-php ch_db:recreate_foreign_tables"
  changed_when: false
