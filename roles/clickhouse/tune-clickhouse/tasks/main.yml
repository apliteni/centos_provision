- name: Replace users config
  template:
    src: users.xml.j2
    dest: "{{ clickhouse_config_path }}/users.xml"
    mode: '0644'
  notify:
    - restart clickhouse

- name: Update SystemD service if need
  meta: flush_handlers
