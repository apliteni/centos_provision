- name: Copy file encoded by SG to the molecule instance
  copy:
    src: test.php
    dest: /test.php

- name: Test if file encoded by SG works
  command: "{{ php_version }} /test.php"
  register: test_php_result
  failed_when: test_php_result.stdout != 'ok'
    
