- name: Uninstall docker-ce
  package:
    name: 
      - docker-ce
      - docker-cli
      - containerd
    state: absent

- name: Remove unneeded docker-ce.repo
  file:
    dest: "/etc/yum.repos.d/{{ item }}.repo"
    state: absent
  with_items:
    - docker-ce
    - mariadb-mirror

