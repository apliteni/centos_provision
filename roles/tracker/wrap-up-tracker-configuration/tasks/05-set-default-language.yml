- name: Set default tracker language
  command: "{{ kctl_path }} run cli-php system:set_setting default_language {{ tracker_language | default('en') }}"
  changed_when: false
  when: kctl_running_mode == "install"
