# 20% RAM - PHP-FPM
php_fpm_memory_mb: "{{ (0.20 * ansible_memtotal_mb) | int }}"
# 10% RAM - RoadRunner
roadrunner_memory_mb: "{{ (0.10 * ansible_memtotal_mb) | int }}"
# 40% RAM - Redis
redis_memory_mb: "{{ (0.40 * ansible_memtotal_mb) | int }}"
# 10% RAM - MariaDB
mariadb_memory_mb: "{{ (0.10 * ansible_memtotal_mb) | int }}"
# ClickHouse / MariaDB main size: 25% RAM
olap_db_mb: "{{ (0.25 * ansible_memtotal_mb) | int }}"
