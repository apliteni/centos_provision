#!/usr/bin/env powscript


ensure_valid(var_name, validation_methods_string)
  error=$(get_error "${var_name}" "$validation_methods")
  if isset $error
    debug "$var_name: validation error - '${error}'"
    fail $error
