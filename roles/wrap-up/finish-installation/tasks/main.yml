- include_tasks: run-after-install/main.yml
  when: "'upgrade' not in ansible_run_tags"

- include_tasks: "{{ upgrade_checkpoint_path }}"
  loop_control:
    loop_var: upgrade_checkpoint_path
  with_upgrade_checkpoints: 'run-after-upgrade-to'

- include_tasks: run-always/main.yml

