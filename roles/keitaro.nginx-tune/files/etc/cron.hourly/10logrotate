#!/bin/sh

/usr/sbin/logrotate -s /var/lib/logrotate/logrotate.hourly.status /etc/logrotate.hourly.conf
EXITVALUE=$?
if [ $EXITVALUE != 0 ]; then
    /usr/bin/logger -t logrotate "ALERT exited abnormally with [$EXITVALUE]"
fi
exit 0