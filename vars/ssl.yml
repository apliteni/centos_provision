letsencrypt_webroot_path: "{{ keitaro.path }}"
letsencrypt_cert_domains: "{{ ssl_domains.split(',') }}"
letsencrypt_email: "{{ ssl_email | default('') }}"
ssl_dhparam_path: /etc/ssl/certs/dhparam.pem
ssl_selfsigned_cert_path: /etc/ssl/certs/cert.pem
ssl_selfsigned_privkey_path: /etc/ssl/certs/privkey.pem

